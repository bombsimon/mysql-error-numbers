package mysqlerrnum

/*
THIS FILE WAS AUTO GENERATED AT {{ .now }}

Do not edit this file, run "mysql-error-numbers" to re-generate this file with
an updated version.
*/

const (
{{ range $l := .string_literals -}}
	{{ $l.Constant }} ErrorString = "{{ $l.NumberString }}"
{{ end -}}
)

// FromErrorString takes a string and returns an ErrorString.
func FromErrorString(s string) ErrorString {
	switch s {
	{{ range $n := .string_literals -}}
	case "{{ $n.NumberString }}":
		return {{ $n.Constant }}
	{{ end -}}
	default:
		return ErrorString("")
	}
}

// String implements the stringer interface and prints the symbol name from the
// documentation. Note that this is NOT the string value of the error itself. To
// get that just cast to string().
func (es ErrorString) String() string {
	switch es {
	{{ range $n := .string_literals -}}
	case {{ $n.Constant }}:
		return "{{ $n.Symbol }}"
	{{ end -}}
	default:
		return ""
	}
}

// Description returns the additional information from the documentation for a
// given ErrorString.
func (es ErrorString) Description() string {
	switch es {
	{{ range $n := .string_literals -}}
	case {{ $n.Constant }}:
		return "{{ $n.Extra }}"
	{{ end -}}
	default:
		return ""
	}
}
